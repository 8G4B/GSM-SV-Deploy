{
  "name": "gsm-sv-deploy",
  "version": "1.0.0",
  "description": "Deploy application to remote server via SSH with deployment lifecycle hooks",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "jest"
  },
  "keywords": [
    "github-actions",
    "ssh",
    "deploy",
    "deployment"
  ],
  "author": "snowykte0426",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "node-ssh": "^13.1.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "@types/js-yaml": "^4.0.9",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}